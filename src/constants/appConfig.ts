import { APP_DESCRIPTION, APP_NAME, APP_VERSION } from "../../template.config";

// App configuration constants
export const APP_CONFIG = {
  name: APP_NAME,
  version: APP_VERSION,
  description: APP_DESCRIPTION,
} as const;
